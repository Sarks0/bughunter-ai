# Bug Bounty Report Template

> Auto-populated by BugBountyFramework reporting engine

---

## Title
**[SEVERITY] [Vulnerability Type] in [Component] leads to [Impact]**

*Example: [CRITICAL] Stored XSS in Admin Dashboard leads to Admin Account Takeover*

---

## Summary

A [SEVERITY] severity vulnerability was discovered in [TARGET PROGRAM] that allows [IMPACT DESCRIPTION].

[2-3 sentence plain-language summary of what the vulnerability is and its impact.]

---

## Vulnerability Details

| Field | Value |
|-------|-------|
| **Type** | [XSS / SQLi / SSRF / IDOR / RCE / etc.] |
| **CVSS Score** | [X.X] |
| **CVSS Vector** | CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H |
| **Affected URL** | `https://app.target.com/endpoint` |
| **Parameter** | `[parameter name]` |
| **Authentication** | Required / Not Required |

---

## Steps to Reproduce

1. Navigate to `[URL]`
2. [Login as normal user / No auth required]
3. [Submit request with payload]
4. [Observe result]

**Exact HTTP Request:**
```http
POST /api/v1/endpoint HTTP/1.1
Host: app.target.com
Authorization: Bearer [ATTACKER_TOKEN]
Content-Type: application/json

{
  "parameter": "[PAYLOAD]"
}
```

**Response:**
```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "data": "[SENSITIVE DATA / EXECUTION RESULT]"
}
```

---

## Impact

This vulnerability allows an attacker to:
- [Primary impact, e.g., "Execute arbitrary JavaScript in the context of any authenticated user"]
- [Secondary impact, e.g., "Steal session cookies and achieve account takeover"]
- [Business impact, e.g., "Access all user PII including names, emails, and payment information"]

**Real-world exploitation scenario:**
[Describe how an attacker would realistically use this vulnerability]

---

## Proof of Concept

```
[Complete PoC code or curl command that demonstrates the vulnerability]
```

**Evidence:**
- Screenshot: [attached]
- Video: [if applicable]
- Collaborator interaction: [if OOB/blind]

---

## Remediation

**Recommended Fix:**
[Specific, actionable remediation advice]

**Code Example (if applicable):**
```python
# VULNERABLE
query = f"SELECT * FROM users WHERE id = {user_id}"

# FIXED
query = "SELECT * FROM users WHERE id = ?"
cursor.execute(query, (user_id,))
```

**References:**
- [OWASP link]
- [CWE reference]

---

## Timeline

| Date | Event |
|------|-------|
| [DATE] | Vulnerability discovered |
| [DATE] | Report submitted |
| [DATE] | Triaged |
| [DATE] | Fixed |

---

*Report generated by BugBountyFramework v1.0 — h4ckologic*
